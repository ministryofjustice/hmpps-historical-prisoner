---
# Per environment values which override defaults in hmpps-historical-prisoner/values.yaml

generic-service:
  replicaCount: 2

  ingress:
    host: historical-prisoner-dev.prison.service.justice.gov.uk

  env:
    INGRESS_URL: "https://historical-prisoner-dev.prison.service.justice.gov.uk"
    HMPPS_AUTH_URL: "https://sign-in-dev.prison.service.justice.gov.uk/auth"
    HISTORICAL_PRISONER_API_URL: "https://historical-prisoner-api-dev.prison.service.justice.gov.uk"
    TOKEN_VERIFICATION_API_URL: "https://token-verification-api-dev.prison.service.justice.gov.uk"
    ENVIRONMENT_NAME: DEV
    AUDIT_ENABLED: "false"

generic-prometheus-alerts:
  alertSeverity: syscon-alerts-non-prod
